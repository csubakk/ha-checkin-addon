{
  "name": "Caddy Proxy",
  "version": "1.0.0",
  "slug": "caddy_proxy",
  "description": "Egyszerű Caddy reverse proxy Home Assistant és FastAPI addon számára.",
  "startup": "application",
  "boot": "auto",
  "host_network": true,
  "map": ["config:rw"],
  "arch": ["aarch64", "amd64"],
  "options": {},
  "schema": {}
}
